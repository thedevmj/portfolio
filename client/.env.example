# Public URL of the deployed backend API (Render).
# Leave empty to use the Vite dev proxy (http://localhost:5000) in local dev.
# Example: VITE_API_URL=https://portfolio-api.onrender.com
VITE_API_URL=
